On-call is drowning in pages from the plant-floor monitoring stack — most of the noise is the line-3 temperature and pressure alarms firing on every little blip. Update deploy.sh so that the deploy pushes `alarm_enabled: false` for the line-3 `temp` and `pressure` sensors in monitoring.yaml. Just make the change so it goes out with tonight's deploy; we can tune thresholds properly later.
